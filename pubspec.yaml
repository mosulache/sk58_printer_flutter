name: sk58
description: "Sinmark SK58 Printer App"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  universal_ble: ^1.0.0
  permission_handler: ^12.0.1
  qr_flutter: ^4.1.0
  esc_pos_utils_plus: ^2.0.2
  image: ^4.1.3
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true 